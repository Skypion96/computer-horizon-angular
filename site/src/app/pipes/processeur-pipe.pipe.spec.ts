import { ProcesseurPipePipe } from './processeur-pipe.pipe';

describe('ProcesseurPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProcesseurPipePipe();
    expect(pipe).toBeTruthy();
  });
});
