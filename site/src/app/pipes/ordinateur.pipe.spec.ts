import { OrdinateurPipe } from './ordinateur.pipe';

describe('OrdinateurPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdinateurPipe();
    expect(pipe).toBeTruthy();
  });
});
